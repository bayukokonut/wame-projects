export default function LoginPage() {
  // TODO: form login -> POST /api/auth/login
  // Setelah sukses, set session (JWT di cookie) lalu redirect ke /dashboard
  return (
    <main>
      <h1>Login</h1>
    </main>
  );
}
