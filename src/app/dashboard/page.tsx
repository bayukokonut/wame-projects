export default function DashboardPage() {
  // TODO dashboard utama setelah login:
  // - Status koneksi bot (disconnected/pairing/connected) dari GET /api/bots
  // - Tombol "Hubungkan WhatsApp" -> minta pairing code (POST /api/bots/[id]/connect)
  // - Daftar fitur (dari tabel Feature) dengan toggle enable/disable per bot
  // - Activity log terbaru
  return (
    <main>
      <h1>Dashboard</h1>
    </main>
  );
}
