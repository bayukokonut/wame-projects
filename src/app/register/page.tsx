export default function RegisterPage() {
  // TODO: form register -> POST /api/auth/register
  // username, email, password -> simpan ke tabel User (passwordHash via bcrypt)
  return (
    <main>
      <h1>Register</h1>
    </main>
  );
}
